schema_version: 20180708
name: cribl-oci-log-partitioner
version: 0.0.1
runtime: docker
memory: 256
timeout: 120
config:
  BUCKET_NAME: Cribl_SIEM
  FUNCTION_MODE: target_writer
  OBJECT_PREFIX: cribl
  INCLUDE_MINUTE: "true"
  LOG_TYPE_MAP: "{}"
